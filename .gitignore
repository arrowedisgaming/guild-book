node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Database
*.db
*.db-shm
*.db-wal

# Playwright
test-results/
playwright-report/
playwright/.cache/
.playwright-mcp/

# Claude Code / agent tooling
.claude/
CLAUDE.md
AGENTS.md
.agents/
.codex/
.worktrees/

# Personal task list (not documentation)
TODO.md

# Graphify analysis output (local, machine-specific paths)
graphify-out/

# Dev-only auth bypass (local machines only — never committed)
src/lib/server/dev-auto-login.ts

# Source art scans (large, licence-restricted — fetched via scripts/fetch-rwsa-tarot.sh)
assets-src/

# HMTW rulebook source material (copyrighted — supplied locally, not committed)
rulebooks/
*.pdf
HisMajestytheWorm-CRB.pdf
# InDesign brand/style template package (fonts with mixed licences, book art)
Adherent of the Worm Template/

# Locally-supplied credentials — never commit
secrets.*
