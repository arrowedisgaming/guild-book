node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/coverage

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Database
*.db
*.db-shm
*.db-wal
.backups/

# Playwright
test-results/
playwright-report/
playwright/.cache/
.playwright-mcp/

# Local tool state and instructions
.[c]laude/
C[Ll]AUDE.md
AGENTS.md
.agents/
.co[d]ex/
.worktrees/

# Personal task list (not documentation)
TODO.md

# Graphify analysis output (local, machine-specific paths)
graphify-out/

# Dev-only auth bypass (local machines only — never committed)
src/lib/server/dev-auto-login.ts

# Source art scans (large, licence-restricted — fetched via scripts/fetch-rwsa-tarot.sh)
assets-src

# HMTW rulebook source material (copyrighted — supplied locally, not committed)
rulebooks/
*.pdf
HisMajestytheWorm-CRB.pdf
# InDesign brand/style template package (fonts with mixed licences, book art)
Adherent of the Worm Template/

# Locally-supplied credentials — never commit
secrets.*
.superpowers/
local.db.*

# Local-only session notes (/done) — never commit
SESSION_NOTES.md

# Generated by `npx wrangler types` — this repo consumes @cloudflare/workers-types
# instead; the binding shapes are mirrored into src/app.d.ts by hand.
worker-configuration.d.ts
